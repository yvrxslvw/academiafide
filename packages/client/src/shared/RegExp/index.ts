import { login } from './login';
import { password } from './password';
import { email } from './email';
import { link } from './link';
import { linkWithText } from './linkWithText';

export const RegExp = { login, password, email, link, linkWithText };
