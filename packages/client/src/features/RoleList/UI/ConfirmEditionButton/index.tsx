import { FC } from 'react';
import { Button } from 'shared/UI';

interface ConfirmEditionButtonProps {}

export const ConfirmEditionButton: FC<ConfirmEditionButtonProps> = () => {
	return <Button>Изменить</Button>;
};
